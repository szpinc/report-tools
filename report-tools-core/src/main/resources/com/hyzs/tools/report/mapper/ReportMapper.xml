<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyzs.tools.report.mapper.ReportMapper">

    <!--
    /**
     * 代码自动生成，请不要修改，切记！！！
     */
    -->

    <sql id="selectAllPropertiesAsDO">
            select  id id, category_id categoryId, database_id databaseId, description description, name name, pageable pageable, statement statement, form_key formKey, is_deleted isDeleted, gmt_create gmtCreate, gmt_modified gmtModified from report
    </sql>

    <sql id="selectByFieldNamesAsDO">
       select <foreach collection="fieldNames" index="index" item="item"  separator=",">${item}</foreach> from report
    </sql>

    <sql id="selectAsDO">
        <choose>
            <when test="fieldNames==null or fieldNames.length==0">
                  <include refid="selectAllPropertiesAsDO"/>
            </when>
            <otherwise>
                  <include refid="selectByFieldNamesAsDO"/>
            </otherwise>
        </choose>
    </sql>


    <select id="listAll" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>

    <select id="mapAll" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" /> where is_deleted=0
    </select>


    <select id="getById" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" /> where id=#{id} and is_deleted=0
    </select>

    <select id="listByIds" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <select id="mapByIds" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" /> where is_deleted=0 and id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </select>

    <update id="deleteById">
        update  report set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id=#{id}
    </update>

    <update id="deleteByIds">
        update  report set is_deleted=1,gmt_modified=CURRENT_TIMESTAMP where id in (<foreach collection="ids" index="index" item="item"  separator=",">#{item}</foreach>)
    </update>


    <update id="updateNotNullProperties">
        update report set
                <if test="categoryId != null">category_id=#{categoryId},</if>
                <if test="databaseId != null">database_id=#{databaseId},</if>
                <if test="description != null">description=#{description},</if>
                <if test="name != null">name=#{name},</if>
                <if test="pageable != null">pageable=#{pageable},</if>
                <if test="statement != null">statement=#{statement},</if>
                <if test="formKey != null">form_key=#{formKey},</if>
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>

    <update id="updateAllProperties">
        update report set
                category_id=#{categoryId},
                database_id=#{databaseId},
                description=#{description},
                name=#{name},
                pageable=#{pageable},
                statement=#{statement},
                form_key=#{formKey},
                gmt_modified=CURRENT_TIMESTAMP
        where id=#{id}
    </update>


    <insert id="insertAllProperties">
              <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                        SELECT LAST_INSERT_ID() as id
                      </selectKey>

        insert into report
        (
                category_id,
                database_id,
                description,
                name,
                pageable,
                statement,
                form_key,
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                #{categoryId},
                #{databaseId},
                #{description},
                #{name},
                #{pageable},
                #{statement},
                #{formKey},
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>


    <insert id="insertNotNullProperties">
                      <selectKey keyProperty="id" resultType="java.lang.Long" order="AFTER">
                                SELECT LAST_INSERT_ID() as id
                              </selectKey>
        insert into report
        (
                <if test="categoryId!=null">category_id,</if>
                <if test="databaseId!=null">database_id,</if>
                <if test="description!=null">description,</if>
                <if test="name!=null">name,</if>
                <if test="pageable!=null">pageable,</if>
                <if test="statement!=null">statement,</if>
                <if test="formKey!=null">form_key,</if>
                is_deleted,gmt_create,gmt_modified
        )
        values
        (
                <if test="categoryId!=null">#{categoryId},</if>
                <if test="databaseId!=null">#{databaseId},</if>
                <if test="description!=null">#{description},</if>
                <if test="name!=null">#{name},</if>
                <if test="pageable!=null">#{pageable},</if>
                <if test="statement!=null">#{statement},</if>
                <if test="formKey!=null">#{formKey},</if>
                0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
        )
    </insert>

        <insert id="insertAllPropertiesList">
           insert into report
           (
                   category_id,
                   database_id,
                   description,
                   name,
                   pageable,
                   statement,
                   form_key,
                   is_deleted,gmt_create,gmt_modified
                   )
           values
           <foreach collection="list" item="item" index="index" separator="," >
           (
                   #{item.categoryId},
                   #{item.databaseId},
                   #{item.description},
                   #{item.name},
                   #{item.pageable},
                   #{item.statement},
                   #{item.formKey},
                   0,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP
           )
           </foreach>
       </insert>



    <select id="listByProperty" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="countByProperty" resultType="java.lang.Integer">
        select count(1) from report
            where is_deleted=0 and ${propertyName}=#{value}
    </select>

    <select id="mapByProperty" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0 and ${propertyName}=#{value}
    </select>


    <update id="updateNotNullFieldsByProperty">
        update report set
                <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                <if test="updateDO.description != null">description=#{updateDO.description},</if>
                <if test="updateDO.name != null">name=#{updateDO.name},</if>
                <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="updateAllFieldsByProperty">
        update report set
                category_id=#{updateDO.categoryId},
                database_id=#{updateDO.databaseId},
                description=#{updateDO.description},
                name=#{updateDO.name},
                pageable=#{updateDO.pageable},
                statement=#{updateDO.statement},
                form_key=#{updateDO.formKey},
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <update id="deleteByProperty">
        update report set
                is_delete=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0 and ${propertyName}=#{value}
    </update>

    <select id="listByProperties" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="countByProperties" resultType="java.lang.Integer">
        select count(1) from report
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <select id="mapByProperties" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="propertyNames" item="propertyName" index="index">
              and  ${propertyName}=#{values.[${index}]}
            </foreach>
    </select>

    <update id="updateNotNullFieldsByProperties">
        update report set
                <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                <if test="updateDO.description != null">description=#{updateDO.description},</if>
                <if test="updateDO.name != null">name=#{updateDO.name},</if>
                <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="updateAllFieldsByProperties">
        update report set
                category_id=#{updateDO.categoryId}
                database_id=#{updateDO.databaseId}
                description=#{updateDO.description}
                name=#{updateDO.name}
                pageable=#{updateDO.pageable}
                statement=#{updateDO.statement}
                form_key=#{updateDO.formKey}
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <update id="deleteByProperties">
        update report set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
         where is_deleted=0
         <foreach collection="propertyNames" item="propertyName" index="index">
           and  ${propertyName}=#{values.[${index}]}
         </foreach>
    </update>

    <select id="_listByQueryItems" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_mapByQueryItems" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

    <select id="_countByQueryItems" resultType="java.lang.Integer">
        select count(1) from report
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </select>

   <update id="_updateNotNullFieldsByQueryItems">
         update report set
                <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                <if test="updateDO.description != null">description=#{updateDO.description},</if>
                <if test="updateDO.name != null">name=#{updateDO.name},</if>
                <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_updateNotAllFieldsByQueryItems">
         update report set
                category_id=#{updateDO.categoryId}
                database_id=#{updateDO.databaseId}
                description=#{updateDO.description}
                name=#{updateDO.name}
                pageable=#{updateDO.pageable}
                statement=#{updateDO.statement}
                form_key=#{updateDO.formKey}
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <update id="_deleteByQueryItems">
         update report set
                is_deleted=1,
                gmt_modified=CURRENT_TIMESTAMP
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                          #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
    </update>

    <select id="_listByPageQuery" resultType="com.hyzs.tools.report.dbo.ReportDO">
        <include refid="selectAsDO" />
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                  <choose>
                      <when test="item.type.optr=='in' || item.type.optr=='not in'">
                          <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                              #{inItem}
                          </foreach>
                      </when>
                      <otherwise>
                         #{values.[${item.valueKeyName}]}
                      </otherwise>
                  </choose>
              </if>
            </foreach>
            <if test="pageQuery.sortFieldNameForDatabase!=null">
                order by ${pageQuery.sortFieldNameForDatabase} ${pageQuery.sortType}
            </if>
            limit #{values.pageOffset},#{pageQuery.pageSize}
    </select>


    <select id="_countByPageQuery" resultType="java.lang.Integer">
        select count(1) from report
            where is_deleted=0
            <foreach collection="items" item="item" index="index">
              and  ${item.fieldName} ${item.type.optr}
              <if test="item.type.hasValue">
                    <choose>
                        <when test="item.type.optr=='in' || item.type.optr=='not in'">
                            <foreach collection="values[item.valueKeyName]" item="inItem" close=")" open="(" separator=",">
                                #{inItem}
                            </foreach>
                        </when>
                        <otherwise>
                           #{values.[${item.valueKeyName}]}
                        </otherwise>
                    </choose>
                </if>
            </foreach>
    </select>



          <select id="listByCategoryId" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and category_id=#{categoryId}
          </select>

          <select id="countByCategoryId" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and category_id=#{categoryId}
           </select>

           <select id="mapByCategoryId" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and category_id=#{categoryId}
           </select>

           <update id="updateNotNullFieldsByCategoryId">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and category_id=#{categoryId}
          </update>

          <update id="updateAllFieldsByCategoryId">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and category_id=#{categoryId}
            </update>

            <update id="deleteByCategoryId">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and category_id=#{categoryId}
            </update>



            <!--property in -->

         <select id="listByInCategoryId" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and category_id in
                  <foreach collection="categoryIdList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInCategoryId" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and category_id in
                      <foreach collection="categoryIdList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInCategoryId" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and category_id in
                        <foreach collection="categoryIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInCategoryId">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and category_id in
                       <foreach collection="categoryIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInCategoryId">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and category_id in
                       <foreach collection="categoryIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInCategoryId">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and category_id in
                        <foreach collection="categoryIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByDatabaseId" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and database_id=#{databaseId}
          </select>

          <select id="countByDatabaseId" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and database_id=#{databaseId}
           </select>

           <select id="mapByDatabaseId" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and database_id=#{databaseId}
           </select>

           <update id="updateNotNullFieldsByDatabaseId">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and database_id=#{databaseId}
          </update>

          <update id="updateAllFieldsByDatabaseId">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and database_id=#{databaseId}
            </update>

            <update id="deleteByDatabaseId">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and database_id=#{databaseId}
            </update>



            <!--property in -->

         <select id="listByInDatabaseId" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and database_id in
                  <foreach collection="databaseIdList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInDatabaseId" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and database_id in
                      <foreach collection="databaseIdList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInDatabaseId" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and database_id in
                        <foreach collection="databaseIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInDatabaseId">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and database_id in
                       <foreach collection="databaseIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInDatabaseId">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and database_id in
                       <foreach collection="databaseIdList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInDatabaseId">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and database_id in
                        <foreach collection="databaseIdList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByDescription" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and description=#{description}
          </select>

          <select id="countByDescription" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and description=#{description}
           </select>

           <select id="mapByDescription" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and description=#{description}
           </select>

           <update id="updateNotNullFieldsByDescription">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and description=#{description}
          </update>

          <update id="updateAllFieldsByDescription">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and description=#{description}
            </update>

            <update id="deleteByDescription">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and description=#{description}
            </update>



            <!--property in -->

         <select id="listByInDescription" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and description in
                  <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInDescription" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and description in
                      <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInDescription" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and description in
                        <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInDescription">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and description in
                       <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInDescription">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and description in
                       <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInDescription">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and description in
                        <foreach collection="descriptionList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByName" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and name=#{name}
          </select>

          <select id="countByName" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and name=#{name}
           </select>

           <select id="mapByName" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and name=#{name}
           </select>

           <update id="updateNotNullFieldsByName">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and name=#{name}
          </update>

          <update id="updateAllFieldsByName">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and name=#{name}
            </update>

            <update id="deleteByName">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and name=#{name}
            </update>



            <!--property in -->

         <select id="listByInName" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and name in
                  <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInName" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and name in
                      <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInName" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and name in
                        <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInName">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and name in
                       <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInName">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and name in
                       <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInName">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and name in
                        <foreach collection="nameList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByPageable" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and pageable=#{pageable}
          </select>

          <select id="countByPageable" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and pageable=#{pageable}
           </select>

           <select id="mapByPageable" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and pageable=#{pageable}
           </select>

           <update id="updateNotNullFieldsByPageable">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and pageable=#{pageable}
          </update>

          <update id="updateAllFieldsByPageable">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and pageable=#{pageable}
            </update>

            <update id="deleteByPageable">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and pageable=#{pageable}
            </update>



            <!--property in -->

         <select id="listByInPageable" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and pageable in
                  <foreach collection="pageableList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInPageable" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and pageable in
                      <foreach collection="pageableList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInPageable" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and pageable in
                        <foreach collection="pageableList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInPageable">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and pageable in
                       <foreach collection="pageableList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInPageable">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and pageable in
                       <foreach collection="pageableList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInPageable">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and pageable in
                        <foreach collection="pageableList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByStatement" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and statement=#{statement}
          </select>

          <select id="countByStatement" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and statement=#{statement}
           </select>

           <select id="mapByStatement" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and statement=#{statement}
           </select>

           <update id="updateNotNullFieldsByStatement">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and statement=#{statement}
          </update>

          <update id="updateAllFieldsByStatement">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and statement=#{statement}
            </update>

            <update id="deleteByStatement">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and statement=#{statement}
            </update>



            <!--property in -->

         <select id="listByInStatement" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and statement in
                  <foreach collection="statementList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInStatement" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and statement in
                      <foreach collection="statementList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInStatement" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and statement in
                        <foreach collection="statementList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInStatement">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and statement in
                       <foreach collection="statementList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInStatement">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and statement in
                       <foreach collection="statementList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInStatement">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and statement in
                        <foreach collection="statementList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>
          <select id="listByFormKey" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and form_key=#{formKey}
          </select>

          <select id="countByFormKey" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and form_key=#{formKey}
           </select>

           <select id="mapByFormKey" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                  where is_deleted=0 and form_key=#{formKey}
           </select>

           <update id="updateNotNullFieldsByFormKey">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
               where is_deleted=0 and form_key=#{formKey}
          </update>

          <update id="updateAllFieldsByFormKey">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and form_key=#{formKey}
            </update>

            <update id="deleteByFormKey">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                 where is_deleted=0 and form_key=#{formKey}
            </update>



            <!--property in -->

         <select id="listByInFormKey" resultType="com.hyzs.tools.report.dbo.ReportDO">
              <include refid="selectAsDO" />
                  where is_deleted=0 and form_key in
                  <foreach collection="formKeyList" item="inItem" close=")" open="(" separator=",">
                      #{inItem}
                  </foreach>
          </select>

          <select id="countByInFormKey" resultType="java.lang.Integer">
                select count(1) from report
                where is_deleted=0 and form_key in
                      <foreach collection="formKeyList" item="inItem" close=")" open="(" separator=",">
                          #{inItem}
                      </foreach>
           </select>

           <select id="mapByInFormKey" resultType="com.hyzs.tools.report.dbo.ReportDO">
                <include refid="selectAsDO" />
                    where is_deleted=0 and form_key in
                        <foreach collection="formKeyList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
           </select>

           <update id="updateNotNullFieldsByInFormKey">
              update report set
                      <if test="updateDO.categoryId != null">category_id=#{updateDO.categoryId},</if>
                      <if test="updateDO.databaseId != null">database_id=#{updateDO.databaseId},</if>
                      <if test="updateDO.description != null">description=#{updateDO.description},</if>
                      <if test="updateDO.name != null">name=#{updateDO.name},</if>
                      <if test="updateDO.pageable != null">pageable=#{updateDO.pageable},</if>
                      <if test="updateDO.statement != null">statement=#{updateDO.statement},</if>
                      <if test="updateDO.formKey != null">form_key=#{updateDO.formKey},</if>
                      gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and form_key in
                       <foreach collection="formKeyList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
          </update>

          <update id="updateAllFieldsByInFormKey">
                update report set
                        category_id=#{updateDO.categoryId},
                        database_id=#{updateDO.databaseId},
                        description=#{updateDO.description},
                        name=#{updateDO.name},
                        pageable=#{updateDO.pageable},
                        statement=#{updateDO.statement},
                        form_key=#{updateDO.formKey},
                        gmt_modified=CURRENT_TIMESTAMP
                   where is_deleted=0 and form_key in
                       <foreach collection="formKeyList" item="inItem" close=")" open="(" separator=",">
                           #{inItem}
                       </foreach>
            </update>

            <update id="deleteByInFormKey">
                update report set
                        is_deleted=1,
                        gmt_modified=CURRENT_TIMESTAMP
                    where is_deleted=0 and form_key in
                        <foreach collection="formKeyList" item="inItem" close=")" open="(" separator=",">
                            #{inItem}
                        </foreach>
            </update>

</mapper>

